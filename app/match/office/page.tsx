import { OfficeMatchSearch } from '@/components/match/OfficeMatchSearch';

export const metadata = { title: 'Office Match — PataSpace' };

export default function OfficeMatchPage() {
  return <main className="property-registration-page"><OfficeMatchSearch /></main>;
}
