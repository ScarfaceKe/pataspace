import { HouseMatchSearch } from '@/components/match/HouseMatchSearch';

export const metadata = { title: 'House Match — PataSpace' };

export default function HouseMatchPage() {
  return <main className="property-registration-page"><HouseMatchSearch /></main>;
}
