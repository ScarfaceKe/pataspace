import { SupportTicketForm } from '@/components/support/SupportTicketForm';
export const metadata = { title: 'Contact Support — PataSpace' };
export default function SupportPage() { return <main className="auth-page"><SupportTicketForm /></main>; }
